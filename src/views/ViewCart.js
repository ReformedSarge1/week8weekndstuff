//needs to display each item in the cart, maybe just add one from each item unless we can
//figure out how to add a qty and show how many are in the cart. 

import React from 'react'

export default function ViewCart() {
  return (
    <div>ViewCart</div>
  )
}
